import { useState } from "react";
import { Bell, Check, Clock3, LockKeyhole, Moon, ShieldCheck } from "lucide-react";
import AtlasShell from "@/components/AtlasShell";

export default function Settings() {
  const [quietHours, setQuietHours] = useState(true);
  const [webNotifications, setWebNotifications] = useState(true);
  const [saved, setSaved] = useState(false);
  const save = (event: React.FormEvent) => { event.preventDefault(); setSaved(true); window.setTimeout(() => setSaved(false), 2500); };
  return <AtlasShell><div className="mx-auto max-w-4xl px-5 py-8 sm:px-8 lg:px-12 lg:py-10"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.16em] text-[#1e4ed8]"><ShieldCheck size={15} /> Settings · preferences & quiet hours</div><h2 className="mt-2 text-3xl font-semibold tracking-tight">Delivery preferences & controls</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">Configure when Drip surfaces reminders and which channels may receive them. External delivery stays off until you explicitly enable it.</p><form onSubmit={save} className="mt-8 space-y-6"><section className="rounded-2xl border border-[#e3e8f0] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3 border-b border-[#edf0f5] pb-4"><Moon className="text-[#1e4ed8]" size={19} /><div><h3 className="font-semibold">Quiet hours window</h3><p className="mt-1 text-xs text-slate-400">Suppress non-urgent delivery notifications during rest hours.</p></div></div><label className="mt-5 flex items-center justify-between rounded-xl border border-[#edf0f5] bg-[#fbfcff] p-4"><span className="text-sm font-medium">Enable quiet-hours policy</span><input type="checkbox" checked={quietHours} onChange={(event) => setQuietHours(event.target.checked)} className="h-4 w-4 accent-[#1e4ed8]" /></label>{quietHours && <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold uppercase tracking-[.14em] text-slate-400">Start time<input type="time" defaultValue="22:00" className="mt-2 block w-full rounded-xl border border-[#e3e8f0] bg-white px-3 py-2.5 text-sm font-normal tracking-normal text-slate-700" /></label><label className="text-xs font-semibold uppercase tracking-[.14em] text-slate-400">End time<input type="time" defaultValue="07:30" className="mt-2 block w-full rounded-xl border border-[#e3e8f0] bg-white px-3 py-2.5 text-sm font-normal tracking-normal text-slate-700" /></label></div>}</section><section className="rounded-2xl border border-[#e3e8f0] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3 border-b border-[#edf0f5] pb-4"><Bell className="text-[#1e4ed8]" size={19} /><div><h3 className="font-semibold">Delivery channels</h3><p className="mt-1 text-xs text-slate-400">Choose where Signal Cards and reminders appear.</p></div></div><label className="mt-5 flex items-center justify-between rounded-xl border border-[#edf0f5] bg-[#fbfcff] p-4"><div><p className="text-sm font-medium">Browser WebUI notifications</p><p className="mt-1 text-xs text-slate-400">Surface updates inside this dashboard.</p></div><input type="checkbox" checked={webNotifications} onChange={(event) => setWebNotifications(event.target.checked)} className="h-4 w-4 accent-[#1e4ed8]" /></label><div className="mt-3 flex items-center justify-between rounded-xl border border-amber-100 bg-amber-50/50 p-4"><div><p className="text-sm font-medium text-amber-900">Telegram delivery gate</p><p className="mt-1 text-xs text-amber-700">Disabled in safe mode until explicit delivery consent is granted.</p></div><LockKeyhole className="text-amber-500" size={18} /></div></section><div className="flex items-center justify-between">{saved ? <span className="flex items-center gap-2 text-xs font-semibold text-emerald-700"><Check size={16} /> Preferences saved for this preview</span> : <span className="flex items-center gap-2 text-xs text-slate-400"><Clock3 size={14} /> Changes are local to this preview</span>}<button type="submit" className="inline-flex items-center gap-2 rounded-xl bg-[#173a9b] px-5 py-3 text-sm font-semibold text-white hover:bg-[#123080]"><ShieldCheck size={16} /> Save preferences</button></div></form></div></AtlasShell>;
}

"use client";

import { useEffect, useState } from "react";
import { Check, CircleDollarSign, Shirt, Sparkles } from "lucide-react";

const PREVIEWS = [
  { eyebrow: "Overview", title: "A calm command center", body: "Signals, sources, and next actions in one glance.", Icon: Sparkles, accent: "from-cyan-400/25 to-blue-500/10", metric: "3 active signals", detail: "Evidence-backed · read-only" },
  { eyebrow: "Money review", title: "Catch the quiet leaks", body: "Recurring charges and trial endings, before they surprise you.", Icon: CircleDollarSign, accent: "from-amber-300/25 to-orange-500/10", metric: "$348.00 / year", detail: "2 findings need review" },
  { eyebrow: "Wardrobe", title: "Use what you own", body: "A real rotation built from confirmed pieces and wear data.", Icon: Shirt, accent: "from-violet-300/25 to-fuchsia-500/10", metric: "12 pieces", detail: "Cost per wear tracked" },
];

export function LandingPreviewCarousel() {
  const [active, setActive] = useState(0);
  useEffect(() => { const timer = window.setInterval(() => setActive((current) => (current + 1) % PREVIEWS.length), 6000); return () => window.clearInterval(timer); }, []);
  const preview = PREVIEWS[active] ?? PREVIEWS[0]!;
  const Icon = preview.Icon;
  return <section className="mx-auto max-w-[1120px] px-6 py-14 sm:py-20" aria-labelledby="preview-heading"><div className="grid items-center gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="mb-3 text-[.68rem] font-extrabold uppercase tracking-[.16em] text-[var(--color-accent-deep)]">Inside Atlas</p><h2 id="preview-heading" className="font-display text-3xl font-semibold tracking-[-.045em] text-[var(--color-ink)] sm:text-4xl">Designed to feel like a decision, not another dashboard.</h2><p className="mt-4 max-w-md text-sm leading-relaxed text-[var(--color-ink-muted)]">Every surface is built around the next useful move, with enough context to trust it and enough restraint to stay out of the way.</p><div className="mt-7 flex gap-2" role="tablist" aria-label="Atlas product previews">{PREVIEWS.map((item, index) => <button key={item.eyebrow} type="button" role="tab" aria-selected={active === index} aria-label={`Show ${item.eyebrow} preview`} onClick={() => setActive(index)} className={`h-2 rounded-full transition-all duration-300 ${active === index ? "w-8 bg-[var(--color-accent)]" : "w-2 bg-[var(--color-line-strong)] hover:bg-[var(--color-accent-light)]"}`} />)}</div></div><div className="relative min-h-[340px] overflow-hidden rounded-[2rem] border border-white/15 bg-[#121820] p-3 shadow-[0_30px_90px_rgba(15,23,42,.22)] sm:min-h-[390px] sm:p-5"><div className={`absolute -right-24 -top-24 size-72 rounded-full bg-gradient-to-br ${preview.accent} blur-3xl transition-all duration-500`} /><div className="relative flex h-full flex-col overflow-hidden rounded-[1.45rem] border border-white/[.09] bg-white/[.06] p-5 backdrop-blur-xl sm:p-7"><div className="flex items-center justify-between border-b border-white/[.09] pb-4"><div className="flex items-center gap-2 text-sm font-semibold text-white"><span className="grid size-7 place-items-center rounded-lg bg-white/10"><Icon size={14} /></span> Atlas</div><span className="flex items-center gap-1.5 rounded-full bg-emerald-300/10 px-2.5 py-1 text-[.6rem] font-semibold uppercase tracking-[.12em] text-emerald-200"><span className="size-1.5 rounded-full bg-emerald-300" /> Live</span></div><div className="mt-8 max-w-md"><p className="text-[.62rem] font-semibold uppercase tracking-[.16em] text-cyan-200/70">{preview.eyebrow}</p><h3 className="mt-2 font-display text-3xl font-semibold tracking-[-.04em] text-white sm:text-4xl">{preview.title}</h3><p className="mt-3 text-sm leading-relaxed text-white/55">{preview.body}</p></div><div className="mt-auto grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-white/[.09] bg-white/[.06] p-4"><p className="text-[.62rem] uppercase tracking-[.12em] text-white/40">Current view</p><p className="mt-3 font-display text-xl font-semibold text-white">{preview.metric}</p><p className="mt-1 text-xs text-white/45">{preview.detail}</p></div><div className="rounded-2xl border border-cyan-200/15 bg-cyan-200/[.07] p-4"><p className="flex items-center gap-1.5 text-[.62rem] uppercase tracking-[.12em] text-cyan-100/65"><Check size={12} /> Trust boundary</p><p className="mt-3 text-sm font-semibold text-white">You stay in control.</p><p className="mt-1 text-xs leading-relaxed text-white/45">Atlas prepares. You approve.</p></div></div></div></div></div></section>;
}
